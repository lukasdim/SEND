package dev.send.api.app.strategy.validation;

/**
 * Placeholder validator for graph/spec compatibility.
 *
 * <p>Future responsibility:
 * verify that graph nodes, edges, ports, and node data match the shared node
 * specifications before execution is requested.
 *
 * <p>This should eventually collaborate with:
 * app.strategy.catalog,
 * domain.strategy.spec,
 * and web.strategy.dto graph records.
 *
 * <p>This is request-time validation code.
 */
public class GraphSpecValidator {}
