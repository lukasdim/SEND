services:
  send-back:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - 8080:8080
  send-front:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - 5173:80
