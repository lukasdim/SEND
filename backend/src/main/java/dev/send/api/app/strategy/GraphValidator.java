package dev.send.api.app.strategy;

/**
 * Placeholder for graph validation logic.
 *
 * <p>Future responsibility:
 * validate incoming graph payloads against shared JSON node specifications before
 * dispatching execution work.
 *
 * <p>This should eventually collaborate with:
 * domain.strategy.spec models,
 * app.strategy.catalog spec loading,
 * and web.strategy.dto graph records.
 *
 * <p>This is request-time validation code.
 */
public class GraphValidator {}
