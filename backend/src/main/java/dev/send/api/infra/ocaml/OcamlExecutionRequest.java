package dev.send.api.infra.ocaml;

/**
 * Placeholder stdio protocol request model.
 *
 * <p>Future responsibility:
 * represent one Java-to-OCaml execution request containing a request id, graph
 * payload, and optional execution context.
 *
 * <p>This should eventually collaborate with:
 * StrategyExecutionService,
 * OcamlWorkerClient,
 * and the OCaml worker's input decoder.
 *
 * <p>This is bridge protocol metadata.
 */
public final class OcamlExecutionRequest {}
